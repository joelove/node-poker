var core = require('../core');

/**
 * @namespace
 */
var POKER = POKER || {};

(function(POKER) {
  'use strict';

  /**
   * This class is responsible for handling the table status and main turn 
   * logic. It also acts as the main notification handler.
   *
   * @class
   */
  POKER.Table = Base.extend({

    /**
     * @private
     * @param {Array} adtechPlacements
     */ 
    _init: function(options) {
      
    }

  });

}(POKER));
